name: backend
description: A Serverpod backend for the Dart and Flutter web application.
version: 1.0.0

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  serverpod: ^1.2.5
  postgres: ^2.3.2
  redis: ^2.0.0

dev_dependencies:
  build_runner: ^2.1.0
  serverpod_cli: ^1.2.5

executables:
  backend: bin/main.dart